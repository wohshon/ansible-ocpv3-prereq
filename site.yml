# This playbook installs the prereq for a ose installation

#- hosts: ose-servers-all
- hosts: ose-servers-all-registry
  remote_user: root
#  vars:
#    favcolor: blue
  vars_files:
    - vars/vars.yml
    - vars/secrets.yml  
  roles:
    - ose-prereq
